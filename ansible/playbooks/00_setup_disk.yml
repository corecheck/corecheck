---
- name: Setup data disk
  hosts: core
  become: true
  tasks:
    - name: Ensure partition is formatted
      filesystem:
        fstype: ext4
        dev: /dev/sdf
    - name: Ensure file systems is mounted
      mount:
        fstype: ext4
        src: /dev/sdf
        path: /data
        state: mounted